#!/bin/bash

set -euo pipefail

timings='Benchmarks/.timings'

mkdir -p "$timings"
swift package \
    --package-path Benchmarks \
    --allow-writing-to-package-directory \
    benchmark \
    --no-progress \
    --format markdown > "${timings}/benchmark_results.md"
